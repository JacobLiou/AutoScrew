# AGENTS.md — AutoScrew 仓库 AI 助手工作指南

## 概述

本文档定义在本仓库中工作的 **AI 编程助手**（含 GitHub Copilot、Cursor Agent、其他 IDE 内嵌助手等）的专业角色、工作边界、禁止清单与最佳实践。许多团队将 `AGENTS.md` 放在仓库根目录作为**代理可读的操作说明**，与本项目 [claude.md](claude.md) 配合使用。

与 [claude.md](claude.md) 相辅相成：

- **claude.md**：项目原则层（Why / What Not）——安全、范围、合规、语言规范
- **AGENTS.md**：实践操作层（How / Which Files）——具体工作流程、文件决策树、常见任务模式

**何时优先读本文**：实现功能或改代码路径、处理 MES/追溯字段、不确定需求属于本期还是二期、需要按任务类型（A–D）选文档与检查顺序时。

---

## 1. 项目特性与上下文

### 1.1 项目定义

**名称**：AutoScrew / 智能锁附系统  
**领域**：工业制造、产线自动化、实时控制系统  
**范围**：  
- 自动取钉 / 供钉（气吸、硬件驱动）
- 智能电批拧紧（扭矩–角度双控）
- 曲线采集与实时判定（浮锁 / 滑牙 / 斜锁 / 卡钉 / 漏锁等）
- 任务管理（50 组预存、一键下发）
- SN→PN 驱动工艺（扫码关联图片与参数）
- MES / 服务器数据对接与追溯

**当前阶段**（截至 2026.05.07）：**α 阶段**（硬件验证与软件实现）  
**关键验收**见 [doc/SPEC.md](doc/SPEC.md#批验收标准)；关键风险见 [doc/RISK_REGISTER.md](doc/RISK_REGISTER.md)。

### 1.2 技术栈（待确认项）

下列项在规格中**未写死品牌**，**不要编造**具体型号或协议细节。项目确认后可补全：

| 技术要素 | 当前状态 | 影响范围 |
|---------|--------|---------|
| 主控形态 | 待确认（PLC / IPC + 软 PLC / 其他） | 代码生成、实时性假设 |
| 现场总线 | 待确认（EtherCAT / Profinet / 其他） | 通信层设计、驱动 |
| 智能电批 / 拧紧控制器 | 待确认（型号、开放 API） | 曲线读取、参数下发、故障码映射 |
| 供料模组驱动 | 待确认 | 气吸控制、状态反馈 |
| HMI / 显示 | 待确认 | 界面布局、交互逻辑 |
| 视觉模块 | **二期**（非本期） | 无 |
| MES / 服务器 | 待对接（接口由公司 IT 规范） | 报文格式、认证、重传策略 |

**AI 行为**：  
- 在需要技术选型时，标注为「待确认」而非虚构具体型号
- 提议设计时说明"假设"（如「假设主控支持 EtherCAT」）
- 参考 [doc/PRD.md](doc/PRD.md) 和供应商会议记录获取已决议项

### 1.3 权威文档导航

| 文档 | 用途 | 何时改动 |
|------|------|---------|
| [readme.md](readme.md) | 门面、目标、架构、参考链接 | 项目范围或关键目标变更 |
| [doc/SPEC.md](doc/SPEC.md) | V1 需求与 KPI（权威） | 需求评审通过后，与 Excel 保持同步 |
| [doc/PRD.md](doc/PRD.md) | 会议决议（吸嘴快切、供料器尺寸等） | 设计评审、PRD 更新 |
| [doc/RISK_REGISTER.md](doc/RISK_REGISTER.md) | 风险台账与缓解 | 识别新风险或风险状态变化 |
| [doc/VERIFICATION_FAT_SAT.md](doc/VERIFICATION_FAT_SAT.md) | FAT/SAT 检查表 | 与 SPEC 变更同步 |
| [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md) | 数据模型、字段、SN–位绑定、MES 最小集 | **数据字段任何变更优先改此**，再改代码 |
| [doc/*.xls](doc/智能锁附系统开发%20(V1).xls) | 原始需求表（图片 / 详细表格） | 需求溯源；本仓库中的表格应与其对齐 |

---

## 2. AI 助手的专业领域与能力边界

### 2.1 高信心领域（积极生成）

AI 助手在以下领域可主动提议与生成代码：

1. **工业控制代码框架**  
   - 状态机设计（任务调度、锁附周期状态转移）
   - 实时数据采集与缓冲（曲线样本、时间戳管理）
   - 参数配置的序列化与验证（JSON / XML 配置文件、类型检查）
   - 错误码定义与映射（异常分类、日志记录）

2. **曲线处理与判定逻辑**  
   - 扭矩–角度曲线的实时读取与内存管理
   - 歪斜检测（轴线夹角 > 3°）、浮锁 / 滑牙等规则实现
   - 单调性、峰值、阈值判定
   - 单元测试与边界条件覆盖

3. **数据存储与可追溯**  
   - 本地数据库设计（SQLite 或项目选定方案）与 SN–螺钉位索引
   - 文件目录结构（年月日层级、batch 组织）
   - 导出与回溯查询（按 SN、按日期范围等）

4. **MES 接口与异步通信**  
   - RESTful / MQTT / 其他通信框架的接口定义
   - 报文序列化（JSON、Protocol Buffers 等）
   - 重传策略、失败处理与日志
   - **前提**：MES 端接口规范已由公司 IT 提供或在 DATA_AND_TRACE.md 中定义

5. **HMI / 配置界面**  
   - 操作流程梳理（扫 SN → 显示 PN 图 → 逐钉引导 → 结果展示）
   - 状态色设计（待上黄闪、进行中黄、OK 绿、NG 红）
   - 配置表单（任务参数录入、验证、保存）

6. **单元测试与验证**  
   - Mock 硬件驱动与 MES 响应
   - 曲线判定逻辑的测试用例
   - 脱机仿真场景

### 2.2 中等信心领域（提议，需评审）

1. **硬件驱动接口设计**  
   - 假设主控与智能电批、供料器通信方式已定（参考 PRD、技术栈确认）
   - 提议通信协议封装、错误重试、超时处理
   - 不承诺具体硬件驱动（由供应商提供或用户指定）

2. **实时性与性能优化**  
   - 基于假设的采样率（≥ 833 Hz 对标参考；需与电批能力对齐）
   - 缓冲、线程 / 异步模型提议
   - **验证需要**：与实际硬件和负载测试对齐

3. **EHS / 安全机制的代码框架**  
   - 紧急停止（E-Stop）信号处理、状态转移
   - 扭矩保护上下限检查
   - 互锁逻辑的参数与流程
   - **明确**：具体 EHS 需求、安全等级、第三方认证标准由用户与供应商定义

### 2.3 低信心 / 禁止领域（拒绝或标注风险）

1. **虚构硬件与规范**  
   - ❌ **不要编造**智能电批型号、控制器厂商、现场总线协议
   - ❌ **不要臆测** MES 报文格式、认证方案
   - ✅ 正确做法：标注「待确认」、参考 [doc/PRD.md](doc/PRD.md) 或要求补充信息

2. **绕过或削弱安全机制**  
   - ❌ **禁止**建议或实现跳过安全门、急停、互锁、扭矩保护
   - ❌ 不在代码/文档中鼓励带电插拔、违规短接
   - ✅ 如遇到安全疑问，标注「需 EHS 评审」

3. **超出本期范围的承诺**  
   - ❌ 不要将二期视觉模块标为已实现
   - ❌ 不要提议整线节拍平衡（超出本工位范围）
   - ✅ 清晰标注功能何时引入（本期 / 二期 / 待评估）

4. **违反数据与追溯契约**  
   - ❌ 自行修改 MES 字段、采样率、SN–位绑定规则而不更新 [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md)
   - ✅ 任何数据模型变更：**先改 DATA_AND_TRACE.md**，再改代码，再视需要更新 FAT/SAT

5. **修改验收数字与阶段日期**  
   - ❌ 如用户未提供新证据，不擅自改 SPEC 中的 KPI 或开发计划
   - ✅ 提出风险或变更建议时，提供数据依据并更新 [doc/RISK_REGISTER.md](doc/RISK_REGISTER.md)

---

## 3. 常见任务与工作流程

### 3.1 任务类型与决策树

#### **Type A：新增功能需求**

**触发场景**：  
- 需求方提出新功能（如「增加参数调整界面」）
- 阶段交付物需补全（如「SN→PN 图片管理」）

**推荐工作流**：

1. **确认需求归属**  
   - 是本期还是二期？见 [doc/SPEC.md](doc/SPEC.md#软件开发需求方案)
   - 涉及数据模型吗？**先检查 [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md)**，必要时补充字段
   - 涉及 MES 接口吗？**先检查 MES 规范**或在 DATA_AND_TRACE.md 占位

2. **提议设计与验证点**  
   ```
   功能：SN→PN 图片显示
   设计假设：
   - 图片存储位置：[待确认，建议 ./config/templates/{PN}/*.jpg]
   - 图片与螺钉位映射：[建议在配置表中定义，见 DATA_AND_TRACE.md]
   - 实时显示性能：[假设 HMI 刷新率 ≥ 30 FPS，需验证]
   验证项：
   - [ ] 与 HMI 框架的集成
   - [ ] 大量图片时的内存管理
   - [ ] FAT 测试覆盖（见 VERIFICATION_FAT_SAT.md）
   ```

3. **生成代码与测试**  
   - 按 [claude.md](claude.md#代码与文档习惯) 风格编写：中文注释 + 英文标识符、匹配现有分层
   - 新增单元测试（尤其关键判定逻辑）
   - 更新相关文档（如 SPEC、FAT/SAT）

4. **文件变更清单**  
   - 代码 / 配置
   - [doc/SPEC.md](doc/SPEC.md) —— 如需更新验收标准
   - [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md) —— 如需新增字段
   - [doc/VERIFICATION_FAT_SAT.md](doc/VERIFICATION_FAT_SAT.md) —— 新增验证项

---

#### **Type B：Bug 修复 / 异常处理**

**触发场景**：  
- 曲线判定误判（如误报浮锁）
- MES 通信超时、报文格式错误
- 数据遗漏或乱序

**推荐工作流**：

1. **定位问题根因**  
   - 收集现象：发生频率、曲线特征、硬件状态日志
   - 检查是否已在 [doc/RISK_REGISTER.md](doc/RISK_REGISTER.md) 中登记
   - 确认是否涉及已知供应商限制（如电批精度 ±3%）

2. **设计修复**  
   - 如果是判定规则，参考 [doc/SPEC.md](doc/SPEC.md#功能与工艺要求) 的标准（如歪斜 > 3°）
   - 如果是数据格式，检查是否违反 [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md)
   - 必要时建议参数调优或硬件诊断（标注为「需现场验证」）

3. **实现与测试**  
   - 补充单元测试（包括原始故障复现）
   - 脱机仿真验证（使用 mock 数据）
   - 标记为待 FAT 验证

---

#### **Type C：MES 集成 / 数据字段变更**

**触发场景**：  
- 上位机新增数据需求（如「需保存吸头编号」）
- MES 字段定义调整（如「SN 长度从 32 改为 64 字符」）

**推荐工作流**（**关键流程**）：

1. **第一步：更新 DATA_AND_TRACE.md**  
   - 新增或修改字段表
   - 更新 SN–位绑定规则、采样率等
   - **此步优先于代码改动**

2. **第二步：评估代码影响**  
   - 数据库 schema（迁移、兼容性）
   - 序列化 / 反序列化（JSON / 二进制格式）
   - MES 报文模板

3. **第三步：更新验收**  
   - [doc/VERIFICATION_FAT_SAT.md](doc/VERIFICATION_FAT_SAT.md) 新增测试用例
   - 可能涉及 [doc/SPEC.md](doc/SPEC.md) 更新（如数据保存格式）

4. **第四步：通知相关方**  
   - MES 侧需同步字段定义
   - 硬件侧如有新采集需求需评估

---

#### **Type D：文档更新 / 规范澄清**

**触发场景**：  
- SPEC 与实现不对齐需澄清
- 新的技术决议（如确认主控为 PLC X 品牌）
- 风险状态变化（如缓解成功、转入新阶段）

**推荐工作流**：

1. **确定文档权威性**  
   - 如果改 SPEC 或 PRD，需要需求方 / 项目经理背书
   - 如果是代码注释或 README，可自行更新
   - 涉及 KPI 或里程碑，**禁止未授权修改**

2. **变更影响评估**  
   - SPEC 变更 → 检查是否影响 FAT/SAT、RISK_REGISTER
   - PRD 变更 → 通知硬件侧、供应商
   - DATA_AND_TRACE 变更 → 触发 Type C 流程

3. **更新所有关联文档**  
   - 示例：如改 KPI 的扭矩精度从 ±3% → ±2%，则需同时改：
     - SPEC.md（技术参数表）
     - VERIFICATION_FAT_SAT.md（验收标准）
     - RISK_REGISTER.md（若引发新风险）

---

### 3.2 代码生成规范

#### **命名与风格**

遵循 [claude.md](claude.md#代码与文档习惯)：

- **标识符**：英文全词，清晰不歧义（如 `torque_nm` 而非 `tor`、`t_m`）
- **注释**：关键逻辑用中文说明；代码本身清晰不需过多注释
- **分层**：匹配现有代码结构，不自行创建新的文件组织方式

**示例**：

```python
# ✅ 好
def detect_skew_angle(torque_curve: List[float], angle_curve: List[float]) -> bool:
    """
    检测螺钉轴线与吸嘴中心轴线夹角是否超过 3°。
    返回 True 表示歪斜，False 表示正常。
    """
    # 实现歪斜检测逻辑...
    pass

# ❌ 避免
def check_skew(tc, ac):  # 标识符缩写不清
    """Check if skew"""  # 中英混杂且无中文说明
    pass
```

#### **测试覆盖**

- 新功能必须有单元测试
- 关键判定逻辑（曲线、歪斜、浮锁等）需边界条件测试
- Mock 硬件驱动与 MES，支持脱机验证

#### **假设与前置条件**

在关键函数或模块顶部标注假设，便于后续维护：

```python
"""
task_scheduler.py

假设：
- 主控支持多线程或异步 I/O
- 智能电批通过 [待确认的通信协议] 传输曲线数据
- 每次锁附的曲线样本数 ≥ 100（需与供应商验证）

前置条件：
- 配置文件已加载且字段完整（见 DATA_AND_TRACE.md）
- MES 连接状态检查由上层负责
"""
```

---

## 4. 常见问题与应答模板

### Q1：「能否增加 X 功能？」

**应答模板**：

```
功能：[名称]
本期范围确认：
- 是本期需求：见 SPEC.md 对应章节（如「软件开发需求方案」「批验收标准」），KPI 为 [具体指标]
- 或二期需求：见 readme.md「范围」小节

设计考虑：
- 依赖的技术栈项：[主控 / 通信 / 存储 等，标注待确认项]
- 与已有功能的交互：[描述]
- 验收方式：[新增 FAT 检查项、修改现有测试等]
- 风险：[如有，更新 doc/RISK_REGISTER.md]

建议流程：
1. 确认需求方背书（SPEC 变更或任务单）
2. 更新 DATA_AND_TRACE.md（如涉及数据）
3. 设计评审与技术栈确认
4. 代码生成与测试
5. FAT 验证
```

---

### Q2：「这个曲线判定结果看起来不对？」

**应答模板**：

```
现象诊断：
- 曲线特征：[上传样本或描述：扭矩波形、角度增长等]
- 预期结果：[浮锁 / 滑牙 / 正常 等]
- 实际结果：[误判情况]

可能原因：
1. 参数设定偏差（如判定阈值），见 [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md) 中对应字段定义
2. 硬件限制（如扭矩精度 ±3%），见 SPEC.md「技术参数」表格
3. 规则本身的边界情况，需补充单元测试

建议：
- [ ] 脱机仿真：用该曲线数据跑本地判定逻辑
- [ ] 检查参数：确认配置文件中的阈值是否与供应商约定一致
- [ ] 现场验证：如脱机结果正常但现场异常，需硬件诊断
- 已登记风险：见 [doc/RISK_REGISTER.md](doc/RISK_REGISTER.md) 台账 [风险 ID]
```

---

### Q3：「MES 同步失败了，怎么处理？」

**应答模板**：

```
MES 集成诊断：
1. 网络检查：见 [MES 侧规范或待对接接口定义]
2. 报文格式：本地已保存，核对 DATA_AND_TRACE.md 中字段与 MES 期望是否一致
3. 重试机制：见代码中的 MES 接口模块
4. 失败日志：提取错误码，对应 [待确认的 MES 错误码表]

当前缓解策略：
- 本地可追溯成功后异步上传（不阻塞生产）
- 失败重试 N 次后进入待人工介入队列
- 参考 [doc/RISK_REGISTER.md](doc/RISK_REGISTER.md) 台账 **R05**（MES 联调窗口）、**R07**（曲线采样与字段）

需要排查的信息：
- [ ] 错误日志（时间戳、报文摘要）
- [ ] MES 侧是否收到报文
- [ ] 网络延迟 / 超时配置
```

---

## 5. 质量与验收要求

### 5.1 代码质量

- **可读性**：新开发者（了解工业控制基础）能在 10 分钟内理解代码意图
- **可维护性**：修改一处功能不需同时改 5+ 文件（低耦合）
- **可测试性**：关键逻辑都有单元测试；脱机仿真场景完整
- **日志**：关键流程、错误、异常都有适当日志级别（DEBUG / INFO / WARNING / ERROR）

### 5.2 文档一致性

| 检查项 | 方法 |
|--------|------|
| 代码与 SPEC 对齐 | 新功能生成后，标注对应的 SPEC 章节与 KPI |
| 数据模型与 DATA_AND_TRACE 同步 | 任何字段变更优先改 DATA_AND_TRACE，再比对代码实现 |
| FAT/SAT 完整性 | SPEC 新增的验收项必须对应 FAT/SAT 检查表中的测试用例 |
| 图表与实现一致 | 源 Excel 表有示意图时，代码实现应符合图示（如 PN 位置标注） |

### 5.3 风险与验证

- **硬件依赖明确**：代码中假设的硬件能力（如采样率、通信延迟）必须在 PRD 或供应商文档中确认
- **脱机可验证**：关键判定逻辑应能用 mock 数据在无硬件环境下运行
- **FAT 覆盖**：所有代码变更对应 FAT 检查表中的至少一项测试

---

## 6. 交接与升级

### 6.1 新 AI 助手或开发者入场

需阅读（优先级）：

1. **本文**（AGENTS.md）——工作边界、任务类型与文档顺序
2. [claude.md](claude.md) —— 项目原则与禁止项
3. [doc/SPEC.md](doc/SPEC.md) —— 完整需求与 KPI
4. [doc/DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md) —— 数据模型
5. 源代码 —— 现有实现参考

### 6.2 工具与能力迭代时的自检

当 **IDE/助手产品升级**、**换用不同模型**、或 **仓库新增主要语言/框架** 时，建议复查：

- 代码风格是否仍符合 [claude.md](claude.md) 规范
- 生成内容中的硬件型号、总线协议、MES 报文是否出现未经证实的「虚构细节」
- 数据模型假设是否仍有效（技术栈或 DATA_AND_TRACE 冻结内容变更后尤需核对）

---

## 附录：快速参考

### 禁止清单（Do Not）

```
❌ 虚构硬件型号、通信协议、MES 报文格式
❌ 绕过 EHS / 安全机制
❌ 修改 SPEC KPI 或阶段日期而无背书
❌ 更改数据字段而不先改 DATA_AND_TRACE.md
❌ 将二期功能标为本期实现
❌ 整行删除关键判定逻辑（即使「简化」代码也要检查验收影响）
❌ 鼓励任何违规硬件操作（带电插拔、短接等）
```

### 必改清单（DO 优先）

```
✅ 新增 / 修改数据字段 → 先改 DATA_AND_TRACE.md
✅ 新增功能 → 检查 SPEC 归属（本期 / 二期）→ 更新 FAT/SAT
✅ 发现 bug → 检查 RISK_REGISTER.md 是否已登记 → 标记修复风险
✅ 整合硬件 / MES → 确认规范已在 PRD 或供应商文档中 → 标注假设
✅ 发布测试版本 → FAT 检查表全项评审 → 生成测试报告
```

### 关键联系（待补充）

| 角色 | 负责范围 | 联系方式 |
|------|---------|--------|
| 需求方 | SPEC 评审、范围确认、KPI 验收 | [待填] |
| 项目经理 | 计划、里程碑、跨部门协调 | [待填] |
| 硬件设计 | 主控、电批、供料器选型、接口规范 | [待填] |
| MES / IT | 接口规范、服务器对接、网络 | [待填] |
| 现场 / FAT | 生产线集成、实际运行验证、用户反馈 | [待填] |

---

## 版本历史

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026.05.07 | 1.0 | 初版发布；基于 claude.md 与 SPEC 规范 |
| 2026.05.08 | 1.1 | 受众扩展为通用 AI 助手；去掉应答模板中的章节符号；强化 Q3 与 RISK 台账引用；6.2 改为工具/模型迭代自检 |

---

**最后更新**：2026.05.08  
**所有者**：AutoScrew 项目团队  
**关联文档**：[claude.md](claude.md) / [SPEC.md](doc/SPEC.md) / [DATA_AND_TRACE.md](doc/DATA_AND_TRACE.md)
