<ui:FluentWindow x:Class="AutoScrew.Hmi.Dialog.ConfirmTips"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title=""
                 MinHeight="300"
                 MinWidth="400"
                 SizeToContent="WidthAndHeight"
                 WindowStyle="None"
                 WindowBackdropType="None"
                 Background="{DynamicResource ShellCardBrush}"
                 MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="QuestionCircle24" Foreground="{DynamicResource ShellAccentBrush}" />
            <TextBlock Text="确认"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="8,0,0,0"
                       VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock x:Name="txt_Content"
                   Grid.Row="1"
                   TextWrapping="Wrap"
                   MaxWidth="320"
                   Margin="0,16"
                   VerticalAlignment="Center" />
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <ui:Button x:Name="btn_OK"
                       Content="{DynamicResource S.Common.Confirm}"
                       Appearance="Primary"
                       MinWidth="100"
                       Margin="0,0,8,0"
                       Click="btn_OK_Click" />
            <ui:Button x:Name="btn_Cancel"
                       Content="{DynamicResource S.Common.Cancel}"
                       Appearance="Secondary"
                       MinWidth="100"
                       Click="btn_Cancel_Click" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
