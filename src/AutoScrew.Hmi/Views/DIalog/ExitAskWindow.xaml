<hc:Window x:Class="AutoScrew.Hmi.Dialog.ExitAskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title=""
        Height="300"
        Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition />
                <RowDefinition Height="90" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Path Margin="20 0 0 0" Fill="{DynamicResource PrimaryBrush}"
                      Stretch="Uniform"
                      ToolTip="DeleteFillCircleGeometry"
                      Data="{StaticResource DeleteFillCircleGeometry}" />
                <TextBlock Text="{DynamicResource S.Common.Warning}"
                           Margin="5 0 0 0"
                           VerticalAlignment="Center" />
            </StackPanel>
            <Grid Grid.Row="1"
                  Margin="20 10">

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource S.ExitAskWindow.ExitAskWindow_Title}"
                               FontSize="22"
                               FontWeight="Bold"
                               TextAlignment="Center"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{DynamicResource S.ExitAskWindow.ExitAskWindow_Content}"
                               Grid.Row="1"
                               TextWrapping="Wrap"
                               MaxWidth="280"
                               TextAlignment="Left"
                               Margin="10 30 10 10"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button x:Name="btn_Confirm"
                        Content="{DynamicResource S.Common.Confirm}"
                        Margin="50 0 0 0"
                        Width="100"
                        Click="btn_Confirm_Click" />
                <Button x:Name="btn_Cancel"
                        Grid.Column="2"
                        Content="{DynamicResource S.Common.Cancel}"
                        Margin="0 0 50 0"
                        Width="100"
                        Click="btn_Cancel_Click" />
            </Grid>
        </Grid>
    </Grid>
</hc:Window>