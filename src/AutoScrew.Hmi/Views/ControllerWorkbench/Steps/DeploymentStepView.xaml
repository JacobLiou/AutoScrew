<UserControl x:Class="AutoScrew.Hmi.Views.ControllerWorkbench.Steps.DeploymentStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:AutoScrew.Hmi.ViewModels"
             d:DataContext="{d:DesignInstance vm:ControllerDeploymentViewModel, IsDesignTimeCreatable=False}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="0,0,0,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <ui:Button Margin="0,0,8,0" Command="{Binding RunSelectedFatCommand}" Content="{DynamicResource S.Workbench.Fat.RunSelected}" />
            <ui:Button Command="{Binding SaveFatStateCommand}" Content="{DynamicResource S.Workbench.Fat.SaveState}" />
        </StackPanel>

        <ui:CardControl Grid.Row="1" Icon="{ui:SymbolIcon CheckmarkCircle24}">
            <ui:CardControl.Header>
                <ui:TextBlock FontTypography="BodyStrong" Text="{DynamicResource S.Workbench.Deployment.Title}" />
            </ui:CardControl.Header>
            <ItemsControl ItemsSource="{Binding FatItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ControllerDeploymentFatItemViewModel}">
                        <Border Margin="0,0,0,8" Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="200" />
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Margin="0,0,12,0" IsChecked="{Binding IsChecked, Mode=TwoWay}" />
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding Title}" TextWrapping="Wrap" />
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                           Text="{Binding LastResult}" TextWrapping="Wrap" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ui:CardControl>

        <TextBlock Grid.Row="2" Margin="0,12,0,0" Text="{Binding StatusMessage}" />
    </Grid>
</UserControl>
