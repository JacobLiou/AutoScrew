<UserControl x:Class="AutoScrew.Hmi.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="clr-namespace:AutoScrew.Hmi.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:AutoScrew.Hmi.ViewModels"
      d:DataContext="{d:DesignInstance vm:SettingsViewModel, IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      d:Background="White"
      mc:Ignorable="d">
    <UserControl.Resources>
        <converters:ThemeToIndexConverter x:Key="ThemeToIndexConverter" />
        <converters:CultureToIndexConverter x:Key="CultureToIndexConverter" />
        <converters:PaneDisplayModeToIndexConverter x:Key="PaneDisplayModeToIndexConverter" />
    </UserControl.Resources>
    <StackPanel Margin="0,0,0,24">
        <ui:TextBlock Margin="0,0,0,8"
                      FontTypography="BodyStrong"
                      Text="{DynamicResource S.Settings.Appearance}" />
        <!--<ui:CardControl Margin="0,0,0,12"
                        Icon="{ui:SymbolIcon Color24}">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ui:TextBlock Grid.Row="0"
                                  FontTypography="Body"
                                  Text="{DynamicResource S.Settings.Theme}" />
                    <ui:TextBlock Grid.Row="1"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{DynamicResource S.Settings.ThemeDesc}" />
                </Grid>
            </ui:CardControl.Header>
            <ComboBox Grid.Column="1"
                      MinWidth="200"
                      SelectedIndex="{Binding ViewModel.CurrentApplicationTheme, Converter={StaticResource ThemeToIndexConverter}, Mode=TwoWay}">
                <ComboBoxItem Content="{DynamicResource S.Common.Light}" />
                <ComboBoxItem Content="{DynamicResource S.Common.Dark}" />
            </ComboBox>
        </ui:CardControl>-->

        <ui:CardControl Margin="0,0,0,12"
                        Icon="{ui:SymbolIcon LocalLanguage24}">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ui:TextBlock Grid.Row="0"
                                  FontTypography="Body"
                                  Text="{DynamicResource S.Settings.Language}" />
                    <ui:TextBlock Grid.Row="1"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{DynamicResource S.Settings.LanguageDesc}" />
                </Grid>
            </ui:CardControl.Header>
            <ComboBox MinWidth="200"
                      SelectedIndex="{Binding ViewModel.SelectedCulture, Converter={StaticResource CultureToIndexConverter}, Mode=TwoWay}">
                <ComboBoxItem Content="{DynamicResource S.Shell.LanguageZh}" />
                <ComboBoxItem Content="{DynamicResource S.Shell.LanguageEn}" />
            </ComboBox>
        </ui:CardControl>

        <ui:TextBlock Margin="0,24,0,8"
                      FontTypography="BodyStrong"
                      Text="{DynamicResource S.Common.About}" />
        <ui:CardExpander ContentPadding="0"
                         IsExpanded="True"
                         Icon="{ui:SymbolIcon Info24}">
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBlock Grid.Row="0"
                                  Grid.Column="0"
                                  FontTypography="Body"
                                  Text="{DynamicResource S.Settings.AboutTitle}" />
                    <ui:TextBlock Grid.Row="1"
                                  Grid.Column="0"
                                  Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                  Text="{DynamicResource S.Settings.AboutCopyright}" />
                    <TextBlock Grid.Row="0"
                               Grid.RowSpan="2"
                               Grid.Column="1"
                               Margin="0,0,16,0"
                               VerticalAlignment="Center"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                               Text="{Binding ViewModel.AppVersion, Mode=OneWay}" />
                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <Grid Margin="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{DynamicResource S.Settings.TechStack}" />
                    <TextBlock Grid.Column="1"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                               Text="{DynamicResource S.Settings.TechDetail}" />
                </Grid>
                <Button Margin="0"
                        Padding="16"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Background="Transparent"
                        BorderThickness="0,1,0,0"
                        Command="{Binding ViewModel.ReportBugCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{DynamicResource S.Settings.BugReport}" />
                        <ui:SymbolIcon Grid.Column="1"
                                       Symbol="Link24" />
                    </Grid>
                </Button>
                <Button Margin="0"
                        Padding="16"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Background="Transparent"
                        BorderThickness="0,1,0,1"
                        Command="{Binding ViewModel.OpenUserManualCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{DynamicResource S.Settings.Docs}" />
                        <ui:SymbolIcon Grid.Column="1"
                                       Symbol="Link24" />
                    </Grid>
                </Button>
            </StackPanel>
        </ui:CardExpander>
    </StackPanel>
</UserControl>
