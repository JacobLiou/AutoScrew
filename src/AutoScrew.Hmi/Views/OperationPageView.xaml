<UserControl x:Class="AutoScrew.Hmi.Views.OperationPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:AutoScrew.Hmi.ViewModels"
             xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:conv="clr-namespace:AutoScrew.Hmi.Converters"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <conv:PathToImageConverter x:Key="PathToImageConverter"/>
    </UserControl.Resources>
    <DockPanel Margin="0">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="扫码 / 输入 SN" Margin="0,0,8,0" Padding="12,6" Command="{Binding OpenScanCommand}"/>
            <TextBox Width="220" Margin="0,0,8,0" VerticalAlignment="Center" Text="{Binding SerialNumberInput, UpdateSourceTrigger=PropertyChanged}" ToolTip="Serial number"/>
            <Button Content="提交 SN" Margin="0,0,8,0" Padding="12,6" Command="{Binding SubmitSnCommand}"/>
            <Button Content="当前螺钉：取钉+拧紧" Margin="0,0,8,0" Padding="12,6" Command="{Binding RunCurrentScrewCommand}"/>
            <Button Content="技术员解锁 NG" Margin="0,0,8,0" Padding="12,6" Command="{Binding UnlockNgCommand}"/>
            <Button Content="复位会话" Margin="0,0,8,0" Padding="12,6" Command="{Binding ResetSessionCommand}"/>
            <Separator Margin="8,0"/>
            <TextBlock Text="角色:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <Button Content="操作员" Margin="0,0,4,0" Padding="8,4" Command="{Binding UseOperatorRoleCommand}"/>
            <Button Content="技术员" Margin="0,0,4,0" Padding="8,4" Command="{Binding UseTechnicianRoleCommand}"/>
            <Button Content="管理员" Margin="0,0,8,0" Padding="8,4" Command="{Binding UseAdminRoleCommand}"/>
            <TextBlock Text="{Binding RoleDisplay}" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </StackPanel>
        <StatusBar DockPanel.Dock="Bottom" Background="#FAFAFA">
            <StatusBarItem>
                <TextBlock Text="{Binding PhaseDisplay}" FontWeight="SemiBold" Margin="0,0,16,0"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap"/>
            </StatusBarItem>
        </StatusBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="4" Margin="0,0,12,0" Background="White">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="900" MaxHeight="560">
                        <Grid Width="{Binding BoardWidth}" Height="{Binding BoardHeight}">
                            <Rectangle Fill="#F0F0F0"/>
                            <Image Stretch="Fill" Opacity="1"
                                   Source="{Binding ProductImagePath, Converter={StaticResource PathToImageConverter}}"/>
                            <ItemsControl ItemsSource="{Binding Markers}" IsHitTestVisible="False">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding CanvasLeft}"/>
                                        <Setter Property="Canvas.Top" Value="{Binding CanvasTop}"/>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:ScrewMarkerVm}">
                                        <Grid Width="{Binding DiameterPx}" Height="{Binding DiameterPx}">
                                            <Ellipse Style="{StaticResource ScrewMarkerStyle}"/>
                                            <TextBlock Text="{Binding Index}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       FontSize="11" FontWeight="Bold" Foreground="#222"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Viewbox>
                </ScrollViewer>
            </Border>
            <Border Grid.Column="1" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="8" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="扭矩–角度（最近一步）" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <sp:WpfPlot x:Name="CurvePlot" Grid.Row="1" MinHeight="220"/>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
