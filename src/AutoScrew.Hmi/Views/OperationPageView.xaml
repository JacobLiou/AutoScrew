<UserControl x:Class="AutoScrew.Hmi.Views.OperationPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:AutoScrew.Hmi.ViewModels"
             xmlns:op="clr-namespace:AutoScrew.Hmi.ViewModels.Operation"
             xmlns:conv="clr-namespace:AutoScrew.Hmi.Converters"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <conv:PathToImageConverter x:Key="PathToImageConverter"/>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <conv:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
    <DockPanel Margin="0">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <Border Margin="0,0,0,8"
                    Padding="10,6"
                    Background="#33E6A23C"
                    BorderBrush="{DynamicResource BrushNg}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Visibility="{Binding IsReworkMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{DynamicResource S.Operation.ReworkBanner}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource BrushNg}" />
            </Border>
            <WrapPanel>
                <ui:TextBox x:Name="SnInputBox"
                            Width="220"
                            Margin="0,0,8,8"
                            VerticalAlignment="Center"
                            PlaceholderText="{DynamicResource S.Operation.SnPlaceholder}"
                            IsEnabled="{Binding IsSnInputEnabled}"
                            Text="{Binding SerialNumberInput, UpdateSourceTrigger=PropertyChanged}"
                            KeyDown="SnInputBox_OnKeyDown"/>
                <ui:Button Content="{DynamicResource S.Operation.SubmitSn}"
                           Margin="0,0,8,8"
                           Style="{StaticResource ShellSecondaryButtonStyle}"
                           Command="{Binding SubmitSnCommand}"/>
                <ui:Button Content="{DynamicResource S.Operation.RunScrew}"
                           Margin="0,0,8,8"
                           Style="{StaticResource ShellSecondaryButtonStyle}"
                           Command="{Binding RunCurrentScrewCommand}"
                           Visibility="{Binding ShowRunScrewButton, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <ui:Button Content="{DynamicResource S.Operation.ConfirmFlip}"
                           Margin="0,0,8,8"
                           Style="{StaticResource ShellSecondaryButtonStyle}"
                           Command="{Binding ConfirmFlipCommand}"/>
                <ui:Button Content="{DynamicResource S.Operation.UnlockNg}"
                           Margin="0,0,8,8"
                           Style="{StaticResource ShellSecondaryButtonStyle}"
                           Command="{Binding UnlockNgCommand}"
                           Visibility="{Binding CanUnlockNgOverlay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <ui:Button Content="{DynamicResource S.Operation.EnterRework}"
                           Margin="0,0,8,8"
                           Style="{StaticResource ShellSecondaryButtonStyle}"
                           Command="{Binding EnterReworkCommand}"
                           Visibility="{Binding ShowTechnicianNgActions, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <ui:Button Content="{DynamicResource S.Operation.ResetSession}"
                           Margin="0,0,8,8"
                           Style="{StaticResource ShellSecondaryButtonStyle}"
                           Command="{Binding ResetSessionCommand}"/>
            </WrapPanel>
            <TextBlock Margin="0,0,0,0"
                       Foreground="{DynamicResource ShellMutedForegroundBrush}"
                       Text="{Binding DeviceProcessPnText}" />
        </StackPanel>
        <Border DockPanel.Dock="Bottom"
                Background="{DynamicResource ShellCardBrush}"
                BorderBrush="{DynamicResource ShellBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,8">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding PhaseDisplay}" FontWeight="SemiBold" Margin="0,0,16,0"/>
                    <TextBlock Text="{Binding GuideHint}" TextWrapping="Wrap" FontWeight="SemiBold"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusMessage}"
                           Margin="0,4,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource ShellMutedForegroundBrush}"/>
            </StackPanel>
        </Border>
        <Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="168" MinWidth="130"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    Background="{DynamicResource ShellCardBrush}"
                    BorderBrush="{DynamicResource ShellBorderBrush}"
                    BorderThickness="0,0,1,0"
                    Padding="8">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="{DynamicResource S.Operation.ProgressTree}"
                               FontWeight="SemiBold"
                               Margin="4,0,0,8"/>
                    <TreeView x:Name="ProgressTree"
                              Focusable="False"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              PreviewMouseLeftButtonDown="ProgressTree_OnPreviewMouseLeftButtonDown"
                              ItemsSource="{Binding ProgressTreeRoots}">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource ShellTreeViewItemStyle}">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Surfaces}">
                                <TextBlock Text="{Binding DisplayLabel}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Screws}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding StateGlyph}"
                                                       Width="16"
                                                       Foreground="{DynamicResource ShellMutedForegroundBrush}"/>
                                            <TextBlock Text="{Binding DisplayLabel}" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <HierarchicalDataTemplate.ItemTemplate>
                                            <DataTemplate DataType="{x:Type op:OperatorScrewNodeViewModel}">
                                                <StackPanel Orientation="Horizontal" Margin="16,0,0,0">
                                                    <TextBlock Text="{Binding StateGlyph}"
                                                               Width="16"
                                                               Foreground="{DynamicResource ShellMutedForegroundBrush}"/>
                                                    <TextBlock Text="{Binding DisplayLabel}" TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </HierarchicalDataTemplate.ItemTemplate>
                                    </HierarchicalDataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </Border>
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="Transparent"/>
            <ui:Card Grid.Column="2" Style="{StaticResource ShellCardStyle}" Margin="0,0,12,0">
                <Grid>
                    <Grid Visibility="{Binding IsCompletionVisible, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <Viewbox Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Grid Width="{Binding BoardWidth}" Height="{Binding BoardHeight}">
                                <Rectangle Fill="#F0F0F0"/>
                                <Image Stretch="Fill"
                                       Source="{Binding ProductImagePath, Converter={StaticResource PathToImageConverter}}"/>
                                <ItemsControl ItemsSource="{Binding Markers}" IsHitTestVisible="False">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <Canvas/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Canvas.Left" Value="{Binding CanvasLeft}"/>
                                            <Setter Property="Canvas.Top" Value="{Binding CanvasTop}"/>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type vm:ScrewMarkerVm}">
                                            <Grid Width="{Binding DiameterPx}" Height="{Binding DiameterPx}">
                                                <Ellipse Style="{StaticResource ScrewMarkerStyle}"/>
                                                <TextBlock Text="{Binding Index}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           FontSize="11" FontWeight="Bold" Foreground="#222"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Viewbox>
                    </Grid>

                    <Grid Visibility="{Binding IsCompletionVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                          Margin="20">
                        <Border CornerRadius="16"
                                Padding="28"
                                Background="#F9FCF8"
                                BorderBrush="#B7E4C7"
                                BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        MaxWidth="560">
                                <TextBlock Text="✓"
                                           FontSize="56"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource BrushOk}"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding CompletionTitle}"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding CompletionSummary}"
                                           Margin="0,10,0,0"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"/>
                                <Border Margin="0,20,0,0"
                                        Padding="16,10"
                                        Background="#EAF7EF"
                                        CornerRadius="10">
                                    <TextBlock Text="{Binding CompletionStats}"
                                               FontFamily="Consolas"
                                               FontSize="14"
                                               HorizontalAlignment="Center"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"/>
                                </Border>
                                <TextBlock Text="{Binding CompletionHint}"
                                           Margin="0,16,0,0"
                                           FontSize="15"
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource ShellMutedForegroundBrush}"/>
                                <ui:Button Content="{DynamicResource S.Operation.CompletionNextSn}"
                                           Margin="0,20,0,0"
                                           HorizontalAlignment="Center"
                                           Appearance="Primary"
                                           Command="{Binding StartNextSnCommand}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </ui:Card>
            <ui:Card Grid.Column="3" Style="{StaticResource ShellCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1.6*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0"
                            BorderBrush="Black"
                            BorderThickness="1"
                            Padding="8">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top"
                                       Text="{DynamicResource S.Operation.CurveTitle}"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8"/>
                            <Grid x:Name="CurvePlotHost" MinHeight="140"/>
                        </DockPanel>
                    </Border>
                    <Border Grid.Row="1"
                            BorderBrush="Black"
                            BorderThickness="1"
                            Padding="8"
                            Margin="0,8,0,0">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top"
                                       Text="{DynamicResource S.Operation.ActivityLog}"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,4"/>
                            <ListBox MinHeight="180"
                                     ItemsSource="{Binding ActivityLog}"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VirtualizingPanel.IsVirtualizing="True"
                                     VirtualizingPanel.VirtualizationMode="Recycling"
                                     ScrollViewer.CanContentScroll="True"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayLine}" TextWrapping="Wrap" FontSize="12"
                                                   Foreground="{DynamicResource ShellMutedForegroundBrush}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </Border>
                </Grid>
            </ui:Card>
            </Grid>
        </Grid>
    </DockPanel>
    <Border Background="#CC000000"
            Visibility="{Binding IsNgOverlayVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
            Panel.ZIndex="100">
        <Border HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="480"
                Padding="24"
                Background="{DynamicResource ShellCardBrush}"
                BorderBrush="{DynamicResource BrushNg}"
                BorderThickness="2"
                CornerRadius="8">
            <StackPanel>
                <TextBlock Text="{Binding NgOverlayTitle}"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{DynamicResource BrushNg}"
                           Margin="0,0,0,12"/>
                <TextBlock Text="{DynamicResource S.Operation.NgOverlayCodeLabel}"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding NgErrorCode}"
                           FontFamily="Consolas"
                           Margin="0,0,0,8"/>
                <TextBlock Text="{DynamicResource S.Operation.NgOverlayScrewLabel}"
                           FontWeight="SemiBold"
                           Visibility="{Binding NgFailedScrewDetail, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                <TextBlock Text="{Binding NgFailedScrewDetail}"
                           TextWrapping="Wrap"
                           FontFamily="Consolas"
                           Margin="0,0,0,8"
                           Visibility="{Binding NgFailedScrewDetail, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                <TextBlock Text="{DynamicResource S.Operation.NgOverlayMessageLabel}"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding NgErrorMessage}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"/>
                <TextBlock Text="{DynamicResource S.Operation.NgOverlayAdviceLabel}"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding NgErrorAdvice}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"/>
                <ui:Button Content="{DynamicResource S.Operation.ParkNg}"
                           HorizontalAlignment="Stretch"
                           Appearance="Primary"
                           Margin="0,0,0,16"
                           Command="{Binding ParkNgCommand}"/>
                <StackPanel Visibility="{Binding ShowOperatorNgActions, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{DynamicResource S.Operation.NgOverlayOperatorHint}"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource ShellMutedForegroundBrush}"
                               Margin="0,0,0,12"/>
                    <ui:Button Content="{DynamicResource S.Operation.EmergencyUnlock}"
                               HorizontalAlignment="Center"
                               Appearance="Secondary"
                               Command="{Binding EmergencyUnlockNgCommand}"/>
                </StackPanel>
                <StackPanel Visibility="{Binding ShowTechnicianNgActions, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{DynamicResource S.Operation.NgOverlayTechnicianHint}"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource ShellMutedForegroundBrush}"
                               Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <ui:Button Content="{DynamicResource S.Operation.UnlockNg}"
                                   Appearance="Secondary"
                                   Margin="0,0,8,0"
                                   Command="{Binding UnlockNgCommand}"
                                   IsEnabled="{Binding CanUnlockNgOverlay}"/>
                        <ui:Button Content="{DynamicResource S.Operation.EnterRework}"
                                   Appearance="Secondary"
                                   Command="{Binding EnterReworkCommand}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
    </Border>
    </Grid>
</UserControl>
