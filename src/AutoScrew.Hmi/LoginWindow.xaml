<Window x:Class="AutoScrew.Hmi.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:vm="clr-namespace:AutoScrew.Hmi.ViewModels"
        mc:Ignorable="d"
        Title="登录 — AutoScrew 作业台"
        Height="440" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        d:DataContext="{d:DesignInstance vm:LoginViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="AutoScrew" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="请使用公司账号登录（开发账号见 appsettings.Development.json）" TextWrapping="Wrap" Opacity="0.75" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="用户名" Margin="0,0,0,4"/>
        <hc:TextBox Grid.Row="3"
                    hc:InfoElement.Placeholder="用户名"
                    Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,0,12"/>

        <TextBlock Grid.Row="4" Text="密码" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordField" Grid.Row="5" Margin="0,0,0,8" Height="36"/>

        <TextBlock Grid.Row="6" Text="{Binding ErrorMessage}" Foreground="#D32F2F" TextWrapping="Wrap" MinHeight="22"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="取消" MinWidth="88" Margin="0,0,8,0" Command="{Binding CancelCommand}"/>
            <Button Content="登录" MinWidth="100" IsDefault="True" Style="{StaticResource ButtonPrimary}" Command="{Binding LoginCommand}"/>
        </StackPanel>
    </Grid>
</Window>
