---
description: 作业台会话与多面模板加载
globs: src/AutoScrew.{Application,Hmi}/**/*
alwaysApply: false
---

# 作业台（Operator）与多面会话

## 加载与推进

- `OperatorSessionController` 使用 `ITemplateLayoutLoader.LoadProductAsync` 加载完整 v2 产品包。
- 首面 = `order` 最小且 `enabled` 的面（`ActiveSurfaceOrdinal = 0`）。
- 本面全部 OK 且有下一面 → `JobSessionPhase.AwaitFlip` → 操作员 `ConfirmAdvanceToNextSurface()` 后进入下一面。
- 全部面 OK → `AllScrewsComplete` → MES 出站（`PositionIndex` = globalIndex）。

## UI 约定

- 左侧进度树只读（严格顺序）；仅 Active 面展开钉列表。
- 翻面使用 `ConfirmTips`；命令按 `JobSessionPhase` 做 `CanExecute` 防呆。
- 订阅 `OperatorSessionController.Changed` 刷新 `MainViewModel`。

## 追溯

- Checkpoint：`SessionCheckpointData` + `SurfaceCheckpointSurface`（含 `surfaceId`）。
- MES 正式 `surface_id` 字段等 IT 定稿后再改 `LockJobResultPayload`；先改 [doc/DATA_AND_TRACE.md](../../doc/DATA_AND_TRACE.md)。
